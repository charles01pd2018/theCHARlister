import type { Props as DisplayHeaderProps,
    SwitchProps as DisplayHeaderSwitchProps,
    Content as DisplayHeaderContent } from './DisplayHeader';

export type {
    DisplayHeaderProps,
    DisplayHeaderSwitchProps,
    DisplayHeaderContent,
}